# Sample documents

Place the PDF, DOCX, or TXT files that you want to index in this folder.

Then either update `DOCUMENT_PATHS` at the top of `ingest_documents.py`, or run:

```bash
python ingest_documents.py --files sample_documents/example.pdf sample_documents/example.docx
```

Do not add confidential documents to a public repository.
