DECLARE
  l_url VARCHAR2(4000);
BEGIN
  l_url := apex_util.prepare_url(
    'f?p=' || :APP_ID || ':14:' || :APP_SESSION ||
    '::NO:14:P14_PROMPT_ID:' || apex_application.g_x01
  );

  apex_json.open_object;
  apex_json.write('url', l_url);
  apex_json.close_object;
END;