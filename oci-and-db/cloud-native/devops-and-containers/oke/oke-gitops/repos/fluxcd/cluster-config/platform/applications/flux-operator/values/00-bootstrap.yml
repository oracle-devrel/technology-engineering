# Resource Manager replaces these placeholders when it first seeds the
# cluster-config repository. Keep this key first in valuesFrom.
image:
  repository: ${region_key}.ocir.io/${tenancy_namespace}/${repo_prefix}/flux-operator
  pullSecrets:
    - name: ocirsecret
