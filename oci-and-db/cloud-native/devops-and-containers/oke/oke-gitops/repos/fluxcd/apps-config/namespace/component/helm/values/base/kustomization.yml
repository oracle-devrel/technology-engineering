apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

labels:
  - pairs:
      reconcile.fluxcd.io/watch: Enabled

configMapGenerator:
  - name: ${component}-values-base
    files:
      - values.yaml=./values.yml

generatorOptions:
  disableNameSuffixHash: true
