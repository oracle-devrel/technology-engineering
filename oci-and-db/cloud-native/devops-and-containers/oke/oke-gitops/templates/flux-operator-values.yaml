# Initial values generated by the Resource Manager bootstrap.
# After the first repository seed, cluster administrators own this file.
# Edit it in Git to configure the self-managed Flux Operator release.
image:
  repository: ${region_key}.ocir.io/${tenancy_namespace}/${repo_prefix}/flux-operator
  pullSecrets:
    - name: ocirsecret
