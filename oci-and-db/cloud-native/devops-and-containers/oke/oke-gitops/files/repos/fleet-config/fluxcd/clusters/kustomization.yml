apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources: []

# Resource Manager generates the selected primary member. Administrators add
# other child directories manually; each cluster reconciles only its own root.
