apiVersion: v1
kind: Service
metadata:
  name: api
  labels:
    app.kubernetes.io/name: reference-app
    app.kubernetes.io/part-of: reference-app
    app.kubernetes.io/component: api
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: reference-app
    app.kubernetes.io/component: api
  ports:
    - name: http
      port: 80
      targetPort: http
