---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.21.0
  name: functionjobs.functions.oci.oracle.com
spec:
  group: functions.oci.oracle.com
  names:
    categories:
    - oci
    - functions
    kind: FunctionJob
    listKind: FunctionJobList
    plural: functionjobs
    singular: functionjob
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.phase
      name: Phase
      type: string
    - jsonPath: .status.succeeded
      name: Succeeded
      type: integer
    - jsonPath: .status.failed
      name: Failed
      type: integer
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: FunctionJob is the Schema for the functionjobs API.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: FunctionJobSpec defines the desired state of FunctionJob.
            properties:
              functionRef:
                description: FunctionRef references a Function in the same namespace.
                properties:
                  name:
                    description: Name is the referenced Function name.
                    minLength: 1
                    type: string
                required:
                - name
                type: object
              parallelism:
                default: 1
                description: Parallelism is the maximum number of payloads invoked
                  during one reconciliation.
                format: int32
                maximum: 100
                minimum: 1
                type: integer
              payload:
                description: Payload is an inline JSON object passed to each invocation.
                type: object
                x-kubernetes-preserve-unknown-fields: true
              payloads:
                description: Payloads is a small ordered set of inline JSON objects
                  passed to invocations.
                items:
                  type: object
                  x-kubernetes-preserve-unknown-fields: true
                maxItems: 100
                minItems: 1
                type: array
                x-kubernetes-list-type: atomic
              retryLimit:
                default: 0
                description: RetryLimit is the number of retries allowed for a failed
                  invocation.
                format: int32
                maximum: 10
                minimum: 0
                type: integer
            required:
            - functionRef
            type: object
            x-kubernetes-validations:
            - message: spec.payload and spec.payloads are mutually exclusive
              rule: '!(has(self.payload) && has(self.payloads))'
          status:
            description: FunctionJobStatus defines the observed state of FunctionJob.
            properties:
              active:
                description: Active is the number of invocations currently in progress.
                format: int32
                type: integer
              completionTime:
                description: CompletionTime is when the job reached a terminal phase.
                format: date-time
                type: string
              conditions:
                description: Conditions contain detailed state transitions.
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              failed:
                description: Failed is the number of failed invocations.
                format: int32
                type: integer
              invocationStatuses:
                description: InvocationStatuses records per-payload invocation status
                  for aggregation and describe output.
                items:
                  description: FunctionJobInvocationStatus records the state of one
                    requested payload invocation.
                  properties:
                    attempts:
                      description: Attempts is the number of attempts made for this
                        invocation.
                      format: int32
                      type: integer
                    completedAt:
                      description: CompletedAt is when this invocation reached a terminal
                        phase.
                      format: date-time
                      type: string
                    error:
                      description: Error contains the last invocation error, if any.
                      type: string
                    index:
                      description: Index is the zero-based requested invocation index.
                      format: int32
                      type: integer
                    invocationId:
                      description: InvocationID is the OCI invocation ID when the
                        invoker returns one.
                      type: string
                    ociRequestId:
                      description: OCIRequestID is the OCI opc-request-id for this
                        invocation when available.
                      type: string
                    payloadDigest:
                      description: PayloadDigest is a stable digest of the inline
                        payload observed for this index.
                      type: string
                    phase:
                      description: Phase is the observed invocation state.
                      enum:
                      - Pending
                      - Running
                      - Succeeded
                      - Failed
                      type: string
                    statusCode:
                      description: StatusCode is an invoker-specific status code when
                        available.
                      format: int32
                      type: integer
                  required:
                  - index
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - index
                x-kubernetes-list-type: map
              lastError:
                description: LastError is the most recent invocation or validation
                  error observed by the controller.
                type: string
              lastOciRequestId:
                description: LastOCIRequestID is the most recent OCI request ID or
                  function call ID observed by the controller.
                type: string
              observedGeneration:
                description: ObservedGeneration is the last metadata.generation observed
                  by the controller.
                format: int64
                type: integer
              phase:
                description: Phase is a compact state summary for kubectl output.
                enum:
                - Pending
                - Running
                - Succeeded
                - Failed
                type: string
              retries:
                description: Retries is the total number of retry attempts used across
                  invocations.
                format: int32
                type: integer
              startTime:
                description: StartTime is when the controller first observed the job.
                format: date-time
                type: string
              succeeded:
                description: Succeeded is the number of successful invocations.
                format: int32
                type: integer
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
