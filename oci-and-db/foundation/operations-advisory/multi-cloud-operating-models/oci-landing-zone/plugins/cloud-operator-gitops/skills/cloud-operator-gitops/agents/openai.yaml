interface:
  display_name: "Cloud Operator GitOps"
  short_description: "OCI onboarding, project repository, and handoff"
  default_prompt: "Use $cloud-operator-gitops to prepare governed OP04 onboarding, validate the handoff, and create the project repository from its pinned template."
