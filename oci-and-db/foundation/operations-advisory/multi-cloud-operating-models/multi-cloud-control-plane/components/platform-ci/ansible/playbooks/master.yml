---
# Master Playbook
# Orchestrates all operations by importing operation-specific playbooks
# Usage: ansible-playbook master.yml --tags <operation_type>

- import_playbook: operations/adb-lifecycle.yml
  tags: [adb-lifecycle]

- import_playbook: operations/deploy-agent.yml
  tags: [deploy-agent]
