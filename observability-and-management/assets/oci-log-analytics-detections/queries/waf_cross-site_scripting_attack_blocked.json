{
  "title": "WAF Cross-Site Scripting Attack Blocked",
  "description": "Detects XSS (Cross-Site Scripting) attempts blocked by OCI WAF. Part of OWASP A03:2021 Injection category.",
  "query": "('Log Source' = 'OCI WAF Logs' or 'Log Source' = 'SOC WAF Security Logs') and (('Action' = 'BLOCK') and ( (('Request URL' like '*<script*' or 'Request URL' like '*<SCRIPT*' or 'Request URL' like '*{'javascript': None}*' or 'Request URL' like '*onerror=*' or 'Request URL' like '*onload=*' or 'Request URL' like '*onmouseover=*' or 'Request URL' like '*onfocus=*' or 'Request URL' like '*onclick=*' or 'Request URL' like '*onsubmit=*' or 'Request URL' like '*<img src=x*' or 'Request URL' like '*<svg onload*' or 'Request URL' like '*<iframe*' or 'Request URL' like '*alert(*' or 'Request URL' like '*prompt(*' or 'Request URL' like '*confirm(*' or 'Request URL' like '*document.cookie*' or 'Request URL' like '*document.location*' or 'Request URL' like '*window.location*' or 'Request URL' like '*eval(*' or 'Request URL' like '*String.fromCharCode*' or 'Request URL' like '*atob(*')) or ('Rule Key' like '*941*') ))",
  "sigma_id": "a1010003-owasp-a03-xss-block-001",
  "level": "high",
  "stig_category": "CAT I",
  "tags": [
    "attack.initial_access",
    "attack.t1189",
    "owasp.a03"
  ],
  "mitre_attack": {
    "tactics": [
      "initial_access"
    ],
    "techniques": [
      "T1189"
    ]
  },
  "logsource": {
    "product": "oci",
    "service": "waf",
    "candidates": [
      "OCI WAF Logs",
      "SOC WAF Security Logs"
    ]
  },
  "falsepositives": [
    "Developers testing JavaScript in form fields",
    "Rich text editors with script content"
  ]
}