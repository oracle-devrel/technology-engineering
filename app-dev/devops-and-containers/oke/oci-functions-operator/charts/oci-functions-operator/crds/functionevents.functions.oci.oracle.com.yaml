---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.21.0
  name: functionevents.functions.oci.oracle.com
spec:
  group: functions.oci.oracle.com
  names:
    categories:
    - oci
    - functions
    kind: FunctionEvent
    listKind: FunctionEventList
    plural: functionevents
    singular: functionevent
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.phase
      name: Phase
      type: string
    - jsonPath: .spec.eventType
      name: Event Type
      type: string
    - jsonPath: .status.matchedTriggers
      name: Matched
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: FunctionEvent is the Schema for Kubernetes-native events routed
          to Functions.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: FunctionEventSpec defines a Kubernetes-native event to route
              to matching FunctionEventTriggers.
            properties:
              eventType:
                description: EventType is the Kubernetes-native event type. It must
                  use the reserved functionevent. prefix.
                minLength: 1
                pattern: ^functionevent\..+
                type: string
              id:
                description: |-
                  ID is an optional user-provided idempotency key carried in the invocation payload.
                  When omitted, the controller uses metadata.uid in the invocation envelope.
                type: string
              payload:
                description: Payload is the event payload passed to the invoked Function
                  inside a FunctionEvent envelope.
                type: object
                x-kubernetes-preserve-unknown-fields: true
              source:
                description: Source identifies the application or component that emitted
                  the event.
                type: string
              subject:
                description: Subject identifies the resource or business object the
                  event is about.
                type: string
              ttlSecondsAfterProcessed:
                description: TTLSecondsAfterProcessed deletes the FunctionEvent this
                  many seconds after it reaches Processed.
                format: int32
                minimum: 0
                type: integer
            required:
            - eventType
            type: object
          status:
            description: FunctionEventStatus defines observed routing and invocation
              state.
            properties:
              conditions:
                description: Conditions contain detailed state transitions.
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              invocations:
                description: Invocations records one invocation per matched trigger.
                items:
                  description: FunctionEventInvocationStatus records one matched trigger
                    invocation.
                  properties:
                    attempts:
                      description: Attempts is the number of invocation attempts made.
                      format: int32
                      type: integer
                    functionName:
                      description: FunctionName is the referenced Function name for
                        the matched trigger.
                      type: string
                    lastAttemptTime:
                      description: LastAttemptTime is the most recent invocation attempt
                        time.
                      format: date-time
                      type: string
                    message:
                      description: Message is the current invocation summary or last
                        stable error.
                      type: string
                    phase:
                      description: Phase is the observed invocation phase.
                      enum:
                      - Pending
                      - Succeeded
                      - Failed
                      type: string
                    triggerName:
                      description: TriggerName is the matched FunctionEventTrigger
                        name.
                      type: string
                  required:
                  - triggerName
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - triggerName
                x-kubernetes-list-type: map
              matchedTriggers:
                description: MatchedTriggers lists matching FunctionEventTrigger names.
                items:
                  type: string
                type: array
                x-kubernetes-list-type: set
              message:
                description: Message is a short human-readable status summary.
                type: string
              observedGeneration:
                description: ObservedGeneration is the last metadata.generation observed
                  by the controller.
                format: int64
                type: integer
              phase:
                description: Phase is a compact state summary for kubectl output.
                enum:
                - Pending
                - Processing
                - Processed
                - Failed
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
