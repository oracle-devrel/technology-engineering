---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.21.0
  name: functioneventtriggers.functions.oci.oracle.com
spec:
  group: functions.oci.oracle.com
  names:
    categories:
    - oci
    - functions
    kind: FunctionEventTrigger
    listKind: FunctionEventTriggerList
    plural: functioneventtriggers
    singular: functioneventtrigger
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.phase
      name: Phase
      type: string
    - jsonPath: .status.ruleId
      name: Rule ID
      type: string
    - jsonPath: .spec.functionRef.name
      name: Function
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: FunctionEventTrigger is the Schema for OCI Events rules that
          invoke OCI Functions.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: FunctionEventTriggerSpec defines an OCI Events rule or FunctionEvent
              route that invokes an OCI Function.
            properties:
              compartmentId:
                description: |-
                  CompartmentID is the compartment OCID where the OCI Events rule is created.
                  Required for OCI Events rule triggers. Omit for FunctionEvent-only triggers.
                pattern: ^ocid1\.compartment\..+
                type: string
              condition:
                description: Condition is the OCI Events matching condition.
                properties:
                  data:
                    description: Data matches OCI event data fields using the same
                      nested shape as the event payload.
                    type: object
                    x-kubernetes-preserve-unknown-fields: true
                  eventType:
                    description: EventType matches OCI eventType values, for example
                      com.oraclecloud.objectstorage.createobject.
                    items:
                      maxLength: 256
                      minLength: 1
                      type: string
                    maxItems: 32
                    minItems: 1
                    type: array
                    x-kubernetes-list-type: set
                  rawJson:
                    description: RawJSON is the exact OCI Events rule condition JSON
                      string.
                    minLength: 2
                    type: string
                type: object
                x-kubernetes-validations:
                - message: condition must set rawJson or at least one structured field
                  rule: has(self.rawJson) || has(self.eventType) || has(self.data)
                - message: condition.rawJson is mutually exclusive with structured
                    condition fields
                  rule: '!has(self.rawJson) || (!has(self.eventType) && !has(self.data))'
              deletionPolicy:
                default: Delete
                description: DeletionPolicy controls whether deleting this Kubernetes
                  object deletes the OCI Events rule.
                enum:
                - Delete
                - Retain
                type: string
              description:
                description: Description describes the OCI Events rule.
                maxLength: 400
                type: string
              displayName:
                description: |-
                  DisplayName is the OCI Events rule display name.
                  Required for OCI Events rule triggers. Omit for FunctionEvent-only triggers.
                maxLength: 255
                minLength: 1
                type: string
              functionRef:
                description: FunctionRef references a Function in the same namespace.
                properties:
                  name:
                    description: Name is the referenced Function name.
                    minLength: 1
                    type: string
                required:
                - name
                type: object
              isEnabled:
                default: true
                description: |-
                  IsEnabled controls whether the OCI Events rule and Function action are enabled.
                  Defaults to true when omitted.
                type: boolean
            required:
            - condition
            - functionRef
            type: object
          status:
            description: FunctionEventTriggerStatus defines the observed state of
              FunctionEventTrigger.
            properties:
              conditions:
                description: Conditions contain detailed state transitions.
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              lastSyncTime:
                description: LastSyncTime is the last time the controller updated
                  observed status.
                format: date-time
                type: string
              message:
                description: Message is a short human-readable status summary.
                type: string
              observedGeneration:
                description: ObservedGeneration is the last metadata.generation observed
                  by the controller.
                format: int64
                type: integer
              phase:
                description: Phase is a compact state summary for kubectl output.
                enum:
                - Pending
                - Ready
                - Error
                type: string
              ruleId:
                description: RuleID is the OCI Events rule OCID.
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
